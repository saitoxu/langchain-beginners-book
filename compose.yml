services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/.venv # Prevent overwriting container's venv with host mount
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    stdin_open: true
    tty: true
    command: bash
